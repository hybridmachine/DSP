<UserControl x:Class="SignalsAndTransforms.Views.About"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SignalsAndTransforms.Views"
             xmlns:res="clr-namespace:SignalsAndTransforms.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding ApplicationInfo}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{x:Static res:Resources.APPLICATION_VERSION_LABEL}"/>
                <TextBlock Text="{Binding AssemblyVersion}"/>
            </StackPanel>
        </StackPanel> 
    </Grid>
</UserControl>
